#ifndef CRYPT_H
#define CRYPT_H

#include <QString>
                                                                //Мокроусов
QString getHash(QString str);
QString crypt(QString str);
QString decrypt(QString str);

#endif // CRYPT_H
